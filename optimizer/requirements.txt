# MiniHSM Optimizer — dependencias Python

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Criptografia y certificados
cryptography>=42.0.0
asn1crypto>=1.5.1

# PAdES (firma PDF)
pyhanko>=0.21.0
pyhanko-certvalidator>=0.26.0

# XAdES (firma XML / facturas)
lxml>=5.1.0

# HTTP cliente (para MiniHSM)
requests>=2.31.0

# Utilidades
python-dotenv>=1.0.0
uharfbuzz  # incrustacion de fuente TTF (sello PAdES Unicode)
